{
  "name": "cypress",
  "version": "1.0.0",
  "description": "This is for cypress API automation practice",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "open": "npx cypress open",
    "start": "npx cypress run --browser electron --headless"
  },
  "author": "Ethan Gabriel",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.6.3"
  },
  "dependencies": {
    "chai-json-schema": "^1.5.1"
  }
}
